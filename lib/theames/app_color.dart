import 'dart:ui';

class AppColors{
  static const primary = Color(0xFFFF9800);  // Blue for primary actions and headers
  static const lightBackground = Color(0xFFFFFFFF);        // White background or text color
  static const lightGray = Color(0xFFF5F5F5);    // Light gray background for cards and sections
  static const darkGray = Color(0xFF4B4B4B);
  static const white=Color(0xFFFFFFFF);

  static const darkBackground = Color(0xFF000000);

}